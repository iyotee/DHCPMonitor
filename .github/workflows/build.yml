name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build Tauri app
        run: npx @tauri-apps/cli build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dhcp-monitor-windows
          path: |
            src-tauri/target/release/bundle/
            src-tauri/target/release/dhcp-monitor.exe
            src-tauri/target/release/packet.dll
            src-tauri/target/release/wpcap.dll

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build Tauri app
        run: npx @tauri-apps/cli build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dhcp-monitor-macos
          path: |
            src-tauri/target/release/bundle/

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libpcap-dev

      - name: Build Tauri app
        run: npx @tauri-apps/cli build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dhcp-monitor-linux
          path: |
            src-tauri/target/release/bundle/
            src-tauri/target/release/dhcp-monitor

  release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: DHCP Monitor ${{ github.ref_name }}
          body: |
            # DHCP Monitor ${{ github.ref_name }}
            
            ## 🚀 Première Release Officielle
            
            Application moderne de surveillance DHCP avec détection spécifique de l'Option 50.
            
            ### 📦 Fichiers Inclus
            
            #### Windows
            - `DHCP Monitor Pro_0.0.0_x64-setup.exe` - Installateur NSIS
            - `DHCP Monitor Pro_0.0.0_x64_en-US.msi` - Package MSI
            - `dhcp-monitor.exe` + DLLs - Version portable
            
            #### macOS
            - `DHCP Monitor Pro.app` - Application macOS
            - `DHCP Monitor Pro_0.0.0_x64.dmg` - Installateur DMG
            
            #### Linux
            - `dhcp-monitor` - Exécutable Linux
            - `DHCP Monitor Pro_0.0.0_amd64.AppImage` - AppImage
            
            ### 🔧 Installation
            
            1. **Windows** : Utiliser l'installateur ou la version portable
            2. **macOS** : Ouvrir le DMG et glisser l'app dans Applications
            3. **Linux** : Exécuter l'AppImage ou l'exécutable
            
            ### ⚠️ Prerequisites
            - Privilèges administrateur/root requis pour la capture réseau
            - Npcap/libpcap inclus dans les installateurs
            
            ### 🎯 Fonctionnalités
            - Capture de paquets DHCP en temps réel
            - Détection intelligente de l'Option 50
            - Interface moderne avec thèmes
            - Statistiques et graphiques
            - Support multiplateforme
            
            ### 🐛 Support
            - Consulter le [README principal](https://github.com/iyotee/DHCPMonitor#readme)
            - Ouvrir une [issue](https://github.com/iyotee/DHCPMonitor/issues) en cas de problème
          files: |
            artifacts/dhcp-monitor-windows/**/*
            artifacts/dhcp-monitor-macos/**/*
            artifacts/dhcp-monitor-linux/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 