name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build Tauri app
        run: |
          cd src-tauri
          npx @tauri-apps/cli build
          
      - name: Create portable package
        shell: cmd
        run: |
          REM Create portable directory in the root
          if exist "portable" rmdir /s /q "portable"
          mkdir "portable"
          
          REM Copy executable
          copy "src-tauri\target\release\dhcp-monitor.exe" "portable\DHCP Monitor Pro.exe"
          
          REM Copy DLLs from System32 (optional - may not exist in GitHub Actions environment)
          if exist "%SystemRoot%\System32\wpcap.dll" (
            copy "%SystemRoot%\System32\wpcap.dll" "portable\" >nul
            echo wpcap.dll copied from System32
          ) else (
            echo wpcap.dll not found in System32
          )
          if exist "%SystemRoot%\System32\packet.dll" (
            copy "%SystemRoot%\System32\packet.dll" "portable\" >nul
            echo packet.dll copied from System32
          ) else (
            echo packet.dll not found in System32
          )
          
          REM Create README
          echo DHCP Monitor Pro - Portable Version > "portable\README.txt"
          echo ================================== >> "portable\README.txt"
          echo. >> "portable\README.txt"
          echo This is a portable version of DHCP Monitor Pro. >> "portable\README.txt"
          echo. >> "portable\README.txt"
          echo Requirements: >> "portable\README.txt"
          echo - Windows 10 or later >> "portable\README.txt"
          echo - Npcap installed (https://npcap.com/) >> "portable\README.txt"
          echo - Administrator privileges to capture packets >> "portable\README.txt"
          echo. >> "portable\README.txt"
          echo Usage: >> "portable\README.txt"
          echo 1. Right-click "DHCP Monitor Pro.exe" >> "portable\README.txt"
          echo 2. Select "Run as administrator" >> "portable\README.txt"
          echo 3. Select your network interface >> "portable\README.txt"
          echo 4. Start capturing DHCP packets >> "portable\README.txt"
          echo. >> "portable\README.txt"
          echo Note: This portable version includes the required Npcap DLLs. >> "portable\README.txt"
          
          echo ✅ Portable package created in "portable" directory

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dhcp-monitor-windows
          path: |
            src-tauri/target/release/bundle/
            src-tauri/target/release/
            portable/
          retention-days: 1

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build Tauri app
        run: |
          cd src-tauri
          npx @tauri-apps/cli build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dhcp-monitor-macos
          path: |
            src-tauri/target/release/bundle/
            src-tauri/target/release/
          retention-days: 1

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libpcap-dev libssl-dev pkg-config libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev libglib2.0-dev

      - name: Build Tauri app
        run: |
          cd src-tauri
          npx @tauri-apps/cli build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dhcp-monitor-linux
          path: |
            src-tauri/target/release/bundle/
            src-tauri/target/release/
          retention-days: 1

  release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
           mkdir -p release-files
           
           # Copier TOUS les fichiers des artifacts
           if [ -d "artifacts/dhcp-monitor-windows" ]; then
             cp -r artifacts/dhcp-monitor-windows/* release-files/ 2>/dev/null || true
           fi
           
           if [ -d "artifacts/dhcp-monitor-macos" ]; then
             cp -r artifacts/dhcp-monitor-macos/* release-files/ 2>/dev/null || true
           fi
           
           if [ -d "artifacts/dhcp-monitor-linux" ]; then
             cp -r artifacts/dhcp-monitor-linux/* release-files/ 2>/dev/null || true
           fi
           
           find release-files -type f -name "*"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: DHCP Monitor ${{ github.ref_name }}
          body: |
            # DHCP Monitor ${{ github.ref_name }}
            
            ## 🚀 Release ${{ github.ref_name }}
            
            Application moderne de surveillance DHCP avec détection spécifique de l'Option 50.
            
            ### 📦 Fichiers Inclus
            
            #### Windows
                         - `DHCP Monitor Pro_1.1.23_x64-setup.exe` - Installateur NSIS
             - `DHCP Monitor Pro_1.1.23_x64_en-US.msi` - Package MSI
            - `dhcp-monitor.exe` + DLLs - Version portable
            
            #### macOS
            - `DHCP Monitor Pro.app` - Application macOS
                         - `DHCP Monitor Pro_1.1.23_x64.dmg` - Installateur DMG
            
            #### Linux
            - `dhcp-monitor` - Exécutable Linux
                         - `DHCP Monitor Pro_1.1.23_amd64.AppImage` - AppImage
            
            ### 🔧 Installation
            
            1. **Windows** : Utiliser l'installateur ou la version portable
            2. **macOS** : Ouvrir le DMG et glisser l'app dans Applications
            3. **Linux** : Exécuter l'AppImage ou l'exécutable
            
            ### ⚠️ Prerequisites
            - Privilèges administrateur/root requis pour la capture réseau
            - Npcap/libpcap inclus dans les installateurs
            
            ### 🎯 Fonctionnalités
            - Capture de paquets DHCP en temps réel
            - Détection intelligente de l'Option 50
            - Interface moderne avec thèmes
            - Statistiques et graphiques
            - Support multiplateforme
            - Vérification automatique des mises à jour
            
            ### 🐛 Support
            - Consulter le [README principal](https://github.com/iyotee/DHCPMonitor#readme)
            - Ouvrir une [issue](https://github.com/iyotee/DHCPMonitor/issues) en cas de problème
          files: release-files/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 